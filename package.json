{
  "name": "ytmp34",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "YTmp3/4 — YouTube media downloader and converter. Privacy-first, ephemeral artifacts.",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev":   "node --env-file=.env --watch --watch-path=src --watch-path=public --watch-path=server.js server.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "compression": "^1.7.5",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.1",
    "helmet": "^8.1.0"
  }
}
